{
  "name": "esbuild-demo",
  "version": "0.0.1",
  "description": "",
  "main": "",
  "scripts": {
    "build": "npx esbuild area.js --bundle --minify --sourcemap --outfile=area.min.js --target=es2018 --global-name=charts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "esbuild": "^0.14.29",
    "eslint": "^8.12.0"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  }
}
